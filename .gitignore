# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build

# misc
.DS_Store
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json

# Firebase Admin — clé privée (ne jamais committer)
serviceAccount.json
scripts/serviceAccount.json
firebase-debug.log
C*UsersManuAppDataLocalTempstars.json

# Firebase Android — téléchargé depuis la console, injecté en CI via un secret
android/app/google-services.json

# Capacitor — APK signés produits localement
*.keystore.properties

# Outillage Claude Code (worktrees, réglages locaux)
.claude/

# Point de terminaison devtools de la WebView, régénéré à chaque session
.wsurl
